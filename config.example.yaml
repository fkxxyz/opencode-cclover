# OpenCode CClover 配置文件
# 位置: ~/.config/opencode-cclover/config.yaml

projects:
  # 示例项目配置
  - name: my-app
    path: /path/to/my-app
    enabled: true
  
  # 可以添加更多项目
  # - name: another-project
  #   path: /path/to/another-project
  #   enabled: true
  
  # 禁用的项目不会启动员工
  # - name: disabled-project
  #   path: /path/to/disabled-project
  #   enabled: false
